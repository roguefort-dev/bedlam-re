# Live playtest — 2026-09-06

Status: incomplete. These observations do not certify a mission or a phase.
The interactive operator is continuing alone; the legacy GLM workers remain
paused at the user's request. No background model configuration was changed.

## Reference session

[black-box observation, high confidence] Original `BEDLAM.EXD` runs under
DOSBox Staging 0.82.2 (5e2ba), normal core, fixed 60000 cycles, svga_s3,
`DOS4GW.EXE BEDLAM.EXD /NOSYNC`. The entire install was copied from the
manifest-verified corpus into `/tmp/bedlam-original-play/BEDLAM`; only that
scratch copy is mounted. Initial setup saved no sound FX and then entered the
game. Audio parity is therefore not established by this session. Mouse capture
was changed to seamless with raw input disabled for desktop control. Relative
uinput mouse events work for the original's mickey-based pointer.

The DOS version is a live comparison, not a replacement for EXW logic canon
(PLAN section 0). No original files or screenshots are committed here.

Observed original journey:

- Title movie can be skipped to the main menu. The menu has a full illustrated
  gold/robot backdrop, a populated player name, and the current difficulty.
- New Single Player Game opens the illustrated mission-selection room.
- Clicking the highlighted Boot Camp region shows mission A-1 briefing text
  and makes the Armoury door available.
- Clicking Armoury opens the equipment room, with weapon art and BUY, CANCEL,
  AUTO, DONE controls. On STANDARD difficulty, the starting balance is 3500.
- AUTO spends that balance and lists PLASMA CANNON X2, BOUNCY GRENADE X6,
  PRESSURE MINE X2, HADES BOMB #1. DONE enters Boot Camp directly.
- Mission entry displays UNIT 1 ARRIVAL IS IMMINENT, then UNIT 1 HAS ARRIVED.
  The sidebar shows those four weapons, with quantities 1800, 0288, 0240,
  and 0003 respectively. These are black-box observations, not yet EXW pins.
- Ground clicks move the robot and the camera follows it. Walking toward the
  paved area near the start opens a welcome/tutorial panel instructing the
  player to approach objects and use a mouse button to dismiss the panel.
- Escape from that state returned to the mission-selection room. This does
  not establish success, a debrief, or a complete mission.

The reliable desktop-control setup uses captured DOSBox mouse input and one
persistent uinput relative mouse device. Creating a new device for each action
can lose events during device registration. Movement and click are sent as
separate steps with visible feedback between them.

Observed remaster journey before the fixes below:

- Release build boots movies and accepts a click on New Single Player Game.
- Menu text appears over black; the original's backdrop is absent.
- New Game enters black Brief and Select scenes. Two further clicks reach the
  ZONEA mission. Those blank screens are missing product behavior, not a hang.
- The mission terrain and sidebar render, but original equipment selection and
  the complete campaign journey have not been demonstrated.

## Fixed: visible mission pointer did not drive canonical click position

[code inspection + regression + live verification, high confidence]
`ShellApp::run_pumps` aligned only the menu cursor with the window pointer.
Mission input instead integrated physical window deltas into canonical game
coordinates. The cursor was also freshly initialized on mission entry and
could disagree with a stationary visible pointer. Scaling, entry and refocus
therefore allowed clicks to target somewhere other than the visible control.

The shell now uses the active mission's current cursor when producing the same
canonical relative InputFrame that the mission already consumes. Scene input
and simulation semantics are unchanged. Existing Fill-mode relative fallback
is unchanged and remains a separate scaling limitation.

Regression `visible_map_button_click_reaches_mission_at_scaled_resolutions`
traverses scenes with mouse edges, loads the real ZONEA corpus, maps the visible
MAP-button position at 1200x900 and 1920x1080, and asserts both canonical cursor
position and map-overlay activation. It failed before the fix: actual cursor
(320,240), expected (560,450). It passes after the fix.

Live verification: rebuilt windowed shell, boot/menu, New Game, two clicks
through the pending blank scenes, then clicked MAP at its visible position.
The strategic-map overlay appeared. This is a control verification, not a
completed mission or a claim of original map-rendering parity.

Validation: cargo fmt; release clippy for bedlam-shell/all-targets with warnings
as errors; shell library tests (146 passed, one existing ignored); bedlam-game
canonical_dump_gate, zone_mission_parity and determinism before and after.
Existing canonical hashes were not changed. MANIFEST verified around corpus
reads. Removed an existing unnecessary `mut` in the touched window test module
so clippy remains warning-free.

## Fixed: missing title-menu backdrop

[EXW-anchored implementation + original comparison + live verification, high confidence]
The menu now stages LOAD_UK/LOAD_US entry zero and LOADPAL/LOADPALU according to
its region, as specified by NameEntryScreen 0x43a5fc..0x43a739 in
RE-EXW-TITLEMENU. Every menu redraw restores the backdrop before drawing text.
The menu owns its backdrop palette, so finishing the title movie cannot recolor
it; the FULLPAL font ramp still overrides entries 224..255. Missing or malformed
assets fail rather than silently presenting a black production menu.

The production-chain regression failed with zero colored pixels above the text
before the change. It now verifies the illustrated region after movie skip and
across hover redraw. A focused unit test checks palette ownership and rejects
malformed/non-640x480 replacements without discarding the valid backdrop.
The rebuilt window visibly displays the same gold/robot artwork seen in DOSBox.
Exact cross-runtime color equality is not claimed.

Validation: shell library 147 passed/one existing ignored; game library 153
passed; release clippy/all-targets and fmt clean; canonical_dump_gate,
zone_mission_parity and determinism passed with no changed pins. Headless smoke
retains scene 696adb1cd110e062, frame cce30c983b97b16d, and audio
110400 frames/158092 nonzero samples. The fetch trace now includes the title
backdrop and palette intentionally. Corpus manifest checks remain clean.

## Shared production controller

The window and deterministic smoke now use ShellController for scene entry,
fixed input pumps, asset staging and visit records. ProductionInput contains
only an InputFrame; the controller exposes its host read-only. Explicit
harness::HarnessController is the sole synthetic SceneAction injection site.
The compatibility smoke remains synthetic evidence, never a gameplay proof.

The window previously staged once after a batch of pumps. The controller now
stages after every pump, before any subsequent input. Failed entry staging
blocks further input until it succeeds. Tests drive the current journey with
real mouse edges, assert mission staging at entry, reject input on a failed
entry, and compile-check that production cannot inject MissionComplete or
mutate the host. These tests do not certify the still-blank Brief/Select UI.

Validation: shell library 149 passed/one existing ignored; two compile-fail
API tests passed; release clippy/all-targets and fmt clean. The compatibility
smoke retains scene 696adb1cd110e062, frame cce30c983b97b16d and audio
110400 frames/158092 nonzero samples. Canonical controls remain unchanged.
The rebuilt window traversed New Game through the existing blank scenes into
Mission, and a visible MAP click opened the strategic overlay.

## Next live work

Restore the illustrated mission-selection and equipment journey from the
EXW-anchored specifications, using the live original sequence above as a
cross-check. Then implement ordinary ground movement and tutorial triggers:
MissionScene currently dispatches viewport clicks only to click_robot, which
ignores empty ground. Continue original Boot Camp to establish objectives,
combat, success and debrief behavior. No mission is complete yet.

## Manual armoury reference

The original's STANDARD Boot Camp balance was 3500. Needler #1 selection
staged one 100-credit/300-ammo pack. Plus repeated up to a sampled
700-credit/2100-ammo cart; Buy produced balance 2800 and one owned row.
Clicking the owned row sold it and opened a replacement cart of the same
quantity. Cancel exposed the refunded balance 3500 and empty inventory.
This distinguishes pending CASH from committed BALANCE and supplies a
concrete sequence for the remaster's future live comparison. The remaster
shop is not yet integrated, so no transaction parity is claimed.

## Live preparation activation

The rebuilt native window now follows New Game → illustrated mission room →
Boot Camp → Armoury → Auto → DONE → loaded Boot Camp using mouse clicks.
The mission HUD lists the purchased Needler, Bouncy Grenade, Sticky Grenade
and Hades Bomb. This run used the default SIMPLE balance 4000; the original
reference used STANDARD 3500, so the differing Auto inventory is not a
same-input comparison. The original sequence remains the behavioral reference.

The production controller regression now drives this route without synthetic
scene actions and checks that the loaded mission receives the purchased
weapon names and ammunition. Shell library: 149 passed, one existing ignored;
release clippy/all-targets and fmt passed. Corpus manifest verified after
the live run. Chassis transfer, intros, subsequent campaign lifecycle,
ground movement and mission completion remain open. Native colors appear
brighter than both the original and the standalone indexed-raster preview;
the GPU fragment shader currently emits display-encoded palette values to
an sRGB surface, a concrete candidate for the next rendering investigation.

## Native palette correction

The brightness candidate above was confirmed by a real GPU readback test:
Rgba8Unorm retained expected expanded VGA bytes while Rgba8UnormSrgb
encoded them again (even 252 became 254; intermediate values were brighter).
The shader now decodes the display-encoded color immediately before writing
to an sRGB attachment. Non-sRGB output retains the existing path; filtering
still operates on expanded palette values. Both full and original expansion
policies are checked across all 64 levels, allowing one byte of GPU rounding.
The test failed before the change and passes after it.

A fresh DOSBox screenshot of the Needler catalogue and a rebuilt native
New Game/Boot Camp/Armoury mouse journey show matching dark artwork tones.
This is a visual comparison, not a pixel-identical frame claim: window scale,
starting balance and pointer state differ. Native title and room also lose
the erroneous brightening. Platform tests: two real GPU tests plus nine
scaling tests pass; release clippy/all-targets, shell build and fmt pass.
Corpus checksum verified before restart and after live comparison.

## Equipment deployment integration

Preparation now stages the separate equipment rows into matching-type robots
in deployment order, consuming shield/battery/damper purchases once and
retaining scanner purchases. The production controller regression checks
resulting shield charges, battery/HP, damper or scanner state against the
actual Auto inventory and verifies removal of consumed rows. A separate
regression prevents duplicating a consumable across same-type robots.

A rebuilt window again followed New Game → Boot Camp → Armoury → Auto →
DONE and displayed the mission. This visual replay proves route continuity;
the controller assertions prove equipment state, which is not directly
visible in the screenshot. Full release game/shell tests passed, including
184 game-library tests, the differential suite, mission parity and shell
controller checks; clippy/all-targets, fmt and release build passed. Corpus
manifest checks are clean. Scanner map behavior and campaign return/recapture
remain incomplete; the journey queue remains open.

## First production ground movement

Left viewport input now stages flag-1 movement commands instead of the
old approximate robot-hit/spread-order action. Held input retargets; sidebar
and map-overlay dispatch remain separate. A real-corpus controller test
clicks ground and observes the deployed Boot Camp robot move. This test
also exposed an entry bug: post-pump mission staging left the scene inactive,
discarding the next input. Loading into Mission now activates immediately.

The rebuilt native window was played through Auto/DONE, then a ground click
at game (240,185) visibly moved the robot upward from its spawn. Unlike the
previous DOSBox observation, the remaster camera remains fixed. Projection
currently uses default zoom 480 and selected robot height as an approximation
to the original averaged camera height. These are remaining fidelity gaps;
ordinary-ground-input acceptance is NOT complete until camera tracking and
click alignment are compared again. Right firing remains unwired.

Validation: game library 184 passed; shell library 149 passed/one existing
ignored; canonical dump, determinism and zone mission parity checks passed
without reference changes before the entry fix; final library checks cover
that fix. Fmt and release clippy/all-targets passed. Manifest checks clean.
The synthetic robot-click unit was replaced with assertions for movement,
no teleport, and no firing; no core canonical replay fixture was changed.

## Ordinary camera follow

The remaster now seeds four selected-robot anchors, advances the ring once
per normal viewport render, averages each coordinate with signed truncation,
and subtracts averaged height from both terrain-camera axes. Left movement
uses that retained camera height. This replaces the previous fixed-camera
and instantaneous-height approximations using EXW 0x40d146..0x40d18d and
0x4039df..0x403b39 (details in RE-EXW-MISSION-ROOM.md).

Native New Game/Boot Camp/Auto/DONE followed by game-coordinate (240,185)
now walks north while the scenery scrolls downward and the robot remains
near the viewport center. This matches the earlier original camera-follow
observation; a fresh original replay was attempted this turn but remained
in the armoury, so no new synchronized trajectory comparison is claimed.
That comparison and tutorial progression remain outstanding.

185 game-library tests and 149 shell-library tests pass (one existing shell
ignore), including exact four-frame camera positions and unchanged sim
state across render-only samples. Fmt, release clippy/all-targets and build
pass; corpus checksum verified. Expected initial cameras in two lifecycle
tests changed from (47,47) to (16,16), reflecting the verified z=31
subtraction. Scripted chase cuts, MP death hold and zoom remain incomplete.

## Additional original Auto sample

Fresh computer-use observation in the original STANDARD Boot Camp armoury:
starting balance 3500, Auto produced Plasma Cannon X1, Needler Cannon #1,
Needler Cannon #2, Hades Bomb #1, Hades Bomb #2, Auto Shielding and Scanner
Level 2, leaving BALANCE 50. This independently confirms that Auto does
not necessarily spend the whole bank; it supports the existing EXW-derived
stop-on-unaffordable top-up behavior. No RNG seed or ammo amounts were
recovered from this screenshot, so this is not a deterministic inventory
golden. The equipment popup also visibly lists Scanner Level 2 at 400/001.

The attempted follow-up mission replay did not reach DONE. Captured-pointer
control was inconsistent after focusing the native window; a later click
sold Scanner Level 2 and opened its one-unit replacement cart (CASH 50),
leaving Auto Shielding owned. The original is still in that shop state.
Restore reliable pointer input, cancel this cart and continue into Boot Camp
for the synchronized movement/tutorial comparison. Do not count this turn
as a new mission-camera or tutorial acceptance run.

## Simulation weapon state and reference-input investigation

The movement-test state dump revealed another production handoff gap:
weapon HUD rows were populated, but robot.weapons and weapon_mask remained
zero. Deployment now also copies IDs/ammo into the simulation slots and
arms the first nonempty group, following EXW 0x40cefd..0x40cf6b. The
controller test checks every matching-type robot against the purchased
loadout. 185 game-library and 149 shell-library tests pass (one existing
ignore); fmt/clippy pass and manifest is clean. Right firing remains unwired,
so no successful live combat is claimed.

Reference-input experiments restarted only the scratch DOSBox session.
Wayland/raw input did not restore reliable movement. Current launch uses
SDL_VIDEODRIVER=x11, SDL_MOUSE_RELATIVE_MODE_WARP=1, mouse_capture=onstart,
mouse_raw_input=false. The cursor visibly moves with the persistent relative
UInput device, but menu clicks still fail to enter New Game reliably; green
hover is not activation proof. The original is now at title/attract, replacing
the old shop checkpoint. Do not describe the input setup as fully repaired.
The current log is /tmp/bedlam-original-play/dosbox-warp.log and config is
/tmp/bedlam-original-play/play.conf. Continue diagnosing clicks alongside
the authorized remaster work; the original tutorial comparison is pending.

## Live weapon state feeds the sidebar

Weapon staging now initializes both simulation and sidebar through the same
entry method. Sidebar row clicks update the simulation mask; after command
consumption, the sidebar mirrors live weapon IDs/ammo and mask and requests
a redraw when they change. This follows the original robot-word readers
and row-bit toggle (EXW 0x408403 and 0x40d659..0x40d712), rather than
leaving pre-mission shop counts frozen in the HUD.

A regression selects only the second of two artillery slots, submits an
existing core firing command, and confirms that only its ammo decreases
and the HUD follows the automatic first-stocked-slot rearm. This is a
command-level regression, not live mouse firing proof. Right-click targeting
and original combat comparison remain open. Game library 186 passed, shell
library 149 passed/one ignored before the added regression; fmt/clippy pass
and corpus checksums are clean. No canonical fixture was rebaselined.

## Right-button ground firing input

Right-button viewport input now emits the ground fallback firing target
using the verified +21 projection, retained camera height and selected
robot. The prior firing regression now uses InputFrame mouse input instead
of injecting CommandRecord, verifies target (80,80,0), selected-slot ammo
consumption and HUD auto-rearm, and confirms the map overlay swallows fire.
The native input adapter already maps Right to bit 2.

This is partial combat integration: renderer-produced critter hot rectangles
and active-structure picking are absent, so every current right-click target
is ground. Simultaneous left/right input currently emits separate movement
and fire records rather than reproducing the original combined-record pointer
quirk; this also needs resolution before input parity is claimed. No new
live combat comparison with DOSBox was completed. Game library 186 passed,
shell 149 passed/one ignored, clippy/fmt and checksum clean; the added overlay
assertion was rerun in the focused weapon-input regression.

## Boot Camp hint state foundation

The new tutorial module models the original mission/mode gate, all 97
hint pad slots, fifteen show-once latches, replacement, 65000-step lifetime,
and command dismissal. Three focused tests pass, including equality at
the movement/fire thresholds, expiry, and revisiting a dismissed hint.
Game crate formatting and clippy with warnings denied pass.

This is not yet connected to MissionScene: text loading, animated border,
trigger timing and robot freeze behavior remain open. It does not constitute
a new native or DOSBox tutorial playthrough.

## Original hint panel renderer

HintPanel now loads all fifteen authored messages in the chosen language
and draws TINYFONT coverage, accents, the measured original box layout,
staggered border/text color ramps and caller-supplied mission darkening
table. The shared room language parser and coverage decoder are reused.
The corpus regression loads and renders every hint in all six languages
(90 panels), checking early/settled colors and untouched outside pixels.
Four tutorial tests, fmt and game-crate clippy pass; corpus checksums pass
before and after.

This renderer is still separate from MissionScene. Trigger/ticker/command
wiring, the active mission dark table and tutorial sound events remain
to be connected before a production welcome-strip playthrough is possible.
These tests prove asset/render execution, not visual parity with DOSBox.

## Hint simulation timing

The shared HintState now lives in bedlam-core. Configured missions probe
the original pad slot inside the walking-state/target gate, before move
math. The robot mover retains the state-3/5 early returns, then an active
hint writes state zero and skips movement (EXW 0x40c570..0x40c58f).
Accepted command records perform hint dismissal before their move/fire
work. The renderer will call the separate hint ticker at the display tail.

A simulation regression starts on a hint pad and proves same-phase stop,
eight-step grace, ninth-step dismissal and resumed movement without a
second hint. All 151 core library tests and the 90-panel game regression
pass, as do library clippy and corpus checksums. All-target core clippy
reports pre-existing warnings in destroy.rs tests and static claim-bank
test data/code (identity operation, type complexity, needless borrow and
manual range check); those unrelated files were left untouched.

Production configuration/loading, display-tail tick/draw and sounds are
still pending. Unconfigured core fixtures retain their previous behavior.
Hint state is not yet represented in the legacy partial mission hash;
this remains a replay/evidence integration limit, not a parity claim.

## Live Boot Camp welcome panel connected

Production mission staging now loads TINYFONT, the configured language
and DARKPAL.PAL before replacing the host mission; Boot Camp configures
the core hint gates. The mission display tail ticks and draws the panel
on both terrain and map-overlay presentations. The production controller
regression walks from deployment to the first welcome strip with ordinary
InputFrame input, observes hint zero, then dismisses it after the grace.

Computer-use replay on the rebuilt native binary completed New Game →
Boot Camp → Armoury → Auto → Done. A viewport click at (336,185)
moved onto the welcome strip and displayed the authored green welcome
panel over the darkened mission background. A subsequent viewport click
removed it and the robot moved onward. This is live native proof.

Both previous live processes had exited. DOSBox was relaunched against
the /tmp scratch install, but captured mouse positioning still prevents
reliable activation of New Game; no fresh original hint screenshot was
obtained. Visual parity remains unproven despite the EXW-anchored layout.
Tutorial SFX and legacy hint-state hash coverage remain open.

Game/shell library suites pass (187 and 149 tests, shell one ignored),
all-target game/shell clippy passes, and corpus checksums pass before and
after tests and native launch. The live native and DOSBox windows remain
open for continuation.

## Fresh original welcome-panel comparison

DOSBox input recovered after ESC to the title, Ctrl+F10 release and
two computer-use clicks, followed by a persistent relative uinput pointer.
Relative movement plus a 300 ms left-button hold successfully selected
Boot Camp, entered Armoury, used Auto and selected Done. Keep the same
relative device alive; captured absolute positioning is unreliable.

On STANDARD, the original shop started at 3500 and Auto left 400, with
Plasma Cannon X2, Bouncy Grenade X4 and Pressure Mine X6. The original
mission HUD showed CASH 000400 and ammo 1800, 0384 and 0640.
The native mission from the preceding live run showed 004000 even though
its shop had ended at 250. This confirms a missing shop-to-mission cash
transfer; unlike the original shared 0x46ae70 money word, native staging
still uses FRESH_CAMPAIGN.

A ground movement click reached the original welcome strip. The settled
original panel visibly matches the native panel's authored text, line
breaks, placement and green border/color: normalized original box origin
approximately (137,203), matching the native panel. Both retain the blank
separator before the dismissal instruction. This is a fresh original
visual reference, not merely a corpus regression. Exact animation/audio
timing has not been measured. The original also shows its full radar/HUD
chrome and water in the lower view, where the native view still has
missing HUD parts and different terrain appearance; those remain open.

Original is left in Boot Camp with welcome panel showing, native after
dismissal nearby. Relative-pointer session remains live for continuation.

The cash discrepancy is fixed: Preparation deployment copies its remaining
shop balance into MissionScene's campaign money, retaining the current
score and requesting the existing score-strip redraw. The production
journey asserts that Auto spent money and the deployed mission exposes
that exact remaining balance. Focused production regression, fmt/clippy
and corpus checksums pass. Live native binary has not yet been rebuilt
for this final cash fix; the welcome-panel comparison used be2192b.

## Boot Camp water edges corrected and checked live

The renderer now interprets DrawParams.zone as zero-based A..G when
selecting off-map tiles. EXW 0x408035's decrement was previously missed,
so A selected invalid-zone fallback sprite 1 (paving). A now selects
0x37+RandB(9), matching the original water. The live rebuilt native
mission now shows water throughout the lower view at spawn, matching
the fresh DOSBox reference. The same live run confirmed shop balance
200 transfers to HUD 000200. Original remains open at its welcome hint.

The terrain/entity frame pins were updated specifically for this verified
edge fix: a326c73fe710e501, 761094cbe33c6b84, 00dc2ec0c12c052a.
The broader scene gate also contained stale fixed-camera and synthetic
click-to-arm/teleport expectations from before the original-input fixes.
It now asserts the height-subtracted spawn camera, target (624,2288),
ordinary state-1 movement and absence of an extraction order, then tests
the map overlay and repeatability. Its frame pins now include correct
water edges and camera history. Spawn sim remains 1cc7b8e125165988;
click/overlay sim pins change because the test now walks instead of
teleporting. No canonical differential fixtures were changed.

Render terrain gate 3/3, game scene gate 3/3, game library 187/187 and
shell library 149/149 (one ignored) pass. All-target render/game/shell
clippy, fmt and corpus checksums pass. Remaining HUD chrome/radar,
tutorial audio and broader combat/progression are still incomplete.

## Original static HUD frame restored

Mission activation now draws GENERAL.BIN sprite 1 at (480,0), opaque,
matching MissionShell 0x447c82..0x447cae. The original initializes two
video buffers; the native retained plane needs one base draw. Dynamic
portraits, bars, rows and money remain on top, and later frames preserve
the base without overwriting widgets.

A fresh computer-use menu/Boot Camp/Armoury/Auto/Done replay on the
rebuilt native binary confirms the numbered row chrome, radar surround,
status surround, and score/cash labels are present. The shop's 250
balance displays as 000250. Comparing with the prior original screenshot,
the frame now matches the missing chrome. Radar and status interiors
remain empty pending their dynamic consumers.

The scene regression compares the entire empty weapon-row band with the
original base sprite and retains widget/overlay/repeatability assertions.
Frame hashes changed only for the newly restored base art; the existing
spawn/click/overlay simulation hashes remain unchanged. Game library
187 tests, shell library 149 (one ignored), scene gate 3 pass, as do
fmt, all-target game/shell clippy and before/after corpus checksums.

## Radar compositing foundation

Scanner now loads the original nineteen-sprite bank and models the
128x128 backing image, seventeen-call refresh/reveal cycle, exact
copy/outline origins, marker transparency and clipping, and mouse
press/hold/release behavior. Marker input is captured only when the
scan refreshes; screen chrome comes from SCANNER sprites 0/17/18.
Release returns the original backdrop-redraw request to its future
mission caller.

Three scanner-specific tests pass (six matching tests overall), covering
cached snapshots, grace-free press/release state, and clipped transparent
markers. Game-crate all-target clippy, fmt and corpus checksums pass.
This module is not yet connected to MissionScene: radar marker producers,
per-frame call ordering and backdrop countdown remain outstanding.

## Live radar connection and DOSBox comparison

The production mission chain now stages SCANNER.BIN before replacing
the current mission, preserving asset-failure retries. MissionScene
calls the scanner on ordinary and overlay frames, retains both mouse
button bits, and services its two-frame backdrop request. Map claim
and equipment-gated strength tiles, active PAD slots, and alive robots
feed the cached image in original order. Squad membership determines
robot color; selection determines the center. Other object/TRT/critter,
arrival and linked-objective marker passes remain unconnected.

Fresh native GUI journey: New Game, Boot Camp, Armoury, Auto, Done.
The formerly empty radar now displays green rings and orange features.
The running DOSBox original shows the same feature arrangement, at a
slightly different robot position; this is a visual comparison, not
a frame-synchronized pixel-parity claim. Native cash was 250 and the
original 400, with different Auto loadouts as previously recorded.

For both applications, moved the actual pointer inside the radar,
held the left button for eight seconds through a temporary uinput
device, and captured the window while held: both show the original
marker legend. After release, both restore the rings and orange
markers, with the expanding refresh box visible in the original.
Original process 1709668 remains running; native process 1774577 runs
the new release build, log /tmp/bedlam-radar-live.log.

Validation: game library 191 tests, shell library 149 tests (one
ignored), game/shell all-target clippy, fmt, release build and corpus
checksums pass. The new marker regression checks pad strict bounds,
zero-position termination, ordering, robot squad color, dead exclusion,
and the exclusive 128-unit range. Existing scanner tests cover cached
refresh timing and press/release state. Broader gameplay parity remains
unproven; this closes the visible empty-radar defect only.

## Next Boot Camp obstacle: destructible world staging

Continued the running original from its radar test. A ground click
northward raised BOOT_CAMP_001 (FENCE GENERATOR). Dismissal resumed
the outstanding movement. With the cursor over the yellow structure
on the upper bank, held right mouse for six seconds: plasma ammo
changed from 1730 to 1608 and score from 25 to 310. The structure
remained visible. This proves the firing/award response, not that
the generator was destroyed or that a fence opened; the impact
recipient was not identified. Do not use this run as a fence-completion
oracle. The original remains at this location for further comparison.

Native ground clicks also moved around the starting shoreline and
scrolled the camera, with its newly connected radar following. It is
now beside the tower east of the starting buildings. The next combat
blocker is independently established from production code: chain.rs
loads no BDG/POS/TRT and host::load_mission stages only the claim bank.
Core's destructible instances, footprints and terrain mirror therefore
remain empty in ordinary play. Canonical harness scenarios explicitly
stage these banks, so their passing results do not prove production
combat. The ground-only firing dispatcher also lacks object picking.

The integration must preserve simulation writes through presentation:
MissionView and MissionSim currently own separate TOT/seen banks;
the view advances LNK animation in its own words, while destruction
and pickups modify the core mirror. Loading the core banks alone
would still leave destroyed buildings drawn. Resolve that ownership
before claiming a visible generator/fence fix. Required follow-up is
production BDG/POS/TRT plus TOT/hazard staging, propagation of world
changes into the view without resetting LNK animation, and the actual
target-picking/impact path. Then repeat the same original/native
interaction through ordinary input.

## Production world loading and terrain propagation

Implemented the prerequisite above (D247): the production chain
preloads and validates BDG/POS/TRT, installs object HP and footprints,
sets the within-zone mission number, initializes TOT/seen and stamps
hazards. Runtime mirror writes now reach MissionView, and the view's
advanced LNK words return to the core after rendering. Destroy score
awards also arm the HUD strip redraw. The observer remains off in
the legacy headless staging paths.

Rebuilt and repeated the native New Game → Boot Camp → Armoury →
Auto → Done journey using the actual window. Moved north from spawn
and held right fire at the small objects on the dark platform for six
seconds. Ammo decreased (1200 to at least 1161 in the captured frame);
score remained zero and no destroyed target or opened fence was
confirmed. World loading and firing ran without a panic. Process
1799045 remains live with log /tmp/bedlam-world-live.log. This is not
yet equivalent to the original generator interaction: native object
hot-rectangle targeting and projectile presentation remain open.

Regression evidence: core library 151, game 192, render 31, shell 149
tests pass (one shell test ignored). The production controller journey
now asserts loaded instances, positive HP, footprints/hazards and TOT
words. A composition test destroys a synthetic object and checks that
its render word is removed; a separate LNK test proves an unrelated
animation continues. Game/shell/render all-target clippy and release
build pass. These tests prove the new connection, not a full gameplay
or generator/fence product verdict.

Broader checks: destroy_gate 25 and mission_scene_gate 3 pass.
canonical_dump_gate remains red in seven corpus cases (six pass).
An isolated tracked-tree export of pre-change HEAD 2238fbe, with a
read-only link to the same corpus, reproduces all seven failures with
identical actual and expected digests. Actual values: S0 9660dca21e25fa46,
S2 df4377749180cff4, S3 87a6fc741f92a341, S4 bac7951d2b803cc5,
S6 96db1e0fb3206c70, S7 7cbedbb21b2f2a19, S8 5bdb330f057a04d4.
Logs: /tmp/bedlam-world-controls.log and
/tmp/bedlam-world-baseline-controls.log. No canonical expected values
were changed. This proves no new digest drift at those failing
assertions; the older drift still needs its own diagnosis and repair.

Determinism passes all four tests. All five zone_mission_parity cases
pass in debug (417.62 seconds); the full scripted-flow case also
passes under `--release --locked --offline` in 27.40 seconds (see
/tmp/bedlam-world-zone-release.log). The debug run completed before
the attempted stop of its duplicate process. Corpus checksums remain
clean. Use release mode for this long rendering battery in future runs.

## Plasma Cannon: placeholder removed, live fire and damage observed

Root cause was deeper than missing sprites: IDs 6/7/8 only spent ammo
and returned a Family placeholder. EXW FUN_0040af98 actually emits
1/2/3 type-5 shots, consumes one ammo per shot, and sets cooldown 2.
Implemented that normal-command path, including extra-shot jitter,
allocation/ammo gates, muzzle height, zero-z horizontal aim, and the
degenerate-aim bookkeeping. See docs/RE-EXW-PLASMA.md for addresses.
The renderer now stages WEAPONS.BIN and draws type-5 shots in the
terrain-sorted list, including startup frames 0/1/2 before the 3..7
loop. Other projectile types and the idle-fire entry remain open.

Fresh native GUI journey again entered Boot Camp through Armoury/Auto.
Moved north from spawn and repeated sustained right fire toward the
small objects on the dark platform. This time blue-white shots were
visible along the firing line, score rose from 0 to 255, and nearby
building art disappeared as destruction reached the render bank.
The captured native frame showed ammo 904 (from 1200), score 255,
cash 250. Process 1829647 remains live; log /tmp/bedlam-plasma-live.log.

Repeated held fire in the still-running original at its yellow-building
position. Its captured frame showed the same blue-white moving shot
sprites, plus large blue-white impact bursts that native still lacks.
Original ammo was 1478 in that frame, score 310, cash 400. The two
positions and prior damage histories differ; no exact score/cadence or
pixel-parity claim follows from these screenshots. The generator/fence
link is still unverified. This does prove ordinary native Plasma Cannon
fire now creates visible shots and applies world damage.

Validation: core library 154, game 192, render 31 plus the new plasma
draw test, shell 149 tests pass (one ignored); the production controller
journey now checks that ordinary right-mouse input creates type-5 shots
and consumes ammo. Weapon-fire integration 28, destroy integration 25,
mission-scene integration 3 pass. Burst tests cover resource exhaustion,
per-shot spending, exact jitter draw count, degenerate aim and untouched
record fields; the draw test covers startup/wrap, projection and offscreen
counter advancement. Clippy, release build and corpus checksums pass.

The release canonical_dump_gate rerun still has six passes and the same
seven failures; all actual/expected digest pairs match the isolated
pre-world baseline recorded above. No expected digest was changed
(/tmp/bedlam-plasma-controls.log).

## Plasma impact burst comparison

Native release process 1844917 (log /tmp/bedlam-impact-live.log): entered
Boot Camp through the actual New Game/region/Armoury/Auto/Done controls,
walked north with ground input, then held right fire toward the dark
platform. Captured ammo 934, score 15, cash 250, with a blue-white impact
burst and spherical shots visible together. The burst was absent before
this fix. Repeated held fire in the original DOSBox session: captured
ammo 1346, score 310, cash 400, with blue-white bursts beside the yellow
structure. These are the same effect family, at different animation
phases and world positions; this is not an exact timing/pixel comparison.
Generator destruction and fence shutdown remain unverified.

The original disburses the still-live type-5 record before clearing it
(RE-EXW-PLASMA.md, 0x410cad). Native now preserves that ordering and feeds
the core debris animation into the production renderer without advancing
it again during presentation. Regression checks cover impact creation,
stored effect position, and single ownership of animation advancement.

Validation: weapon-fire integration 28, game library 193, destruction
integration 25, mission-scene integration 3 pass; fmt, clippy and release
build pass. Canonical replay controls retain six passes and seven failures
with exactly the same actual/expected digest pairs as the recorded
pre-world baseline (/tmp/bedlam-impact-controls.log). No pins changed.
Corpus checksums pass after the live runs.

## Boot Camp generator and linked fence shutdown

Implemented the set-1/mission-1 mapping and timer in RE-EXW-FENCE.md.
Native fresh release /tmp/bedlam-fence-live.log: entered through the
actual menu/Armoury path, toggled the accidentally opened map off,
walked north with ground clicks, aimed at the twin-roof building on
the upper-right platform (window input 714,309), and held right fire.
Captured ammo 718, score 255, cash 250. The generator building vanished
and the two visible white fence runs disappeared, leaving clear grass.
The same native view before fire showed both white fence runs.

Original process 1709668: first fired into the red machinery beside
the starting wall; this only raised score 310 to 320 and did not prove
shutdown. Walked north, then aimed at the twin-roof building near the
right viewport edge. Held fire changed ammo 1124 to 962 and score
320 to 560; that building collapsed. Walking north afterwards raised
the LASER FENCE tutorial explaining generator-based deactivation.
The fence posts remain, with a clear gap and no connecting beams.
Both runs now visibly support the generator/shutdown path. Locations,
prior damage and animation phases differ; no exact timing or score
parity claim follows. Original remains at the laser-fence tutorial.

Validation: core library 156, game library 193, destruction integration
26 and mission-scene integration 3 pass. New real-corpus regression
hits the actual generator through resolve_object_impact and advances
normal frames: 13 linked segments survive eight ticks and all retire
on the ninth, each emitting its origin floor write. Unit tests cover
capacity replacement, mission/network gates, secondary generator,
untouched neighboring layers, no damage-tail score/effects, and no
repeat writes. Release build, fmt, core-library clippy and game/shell
all-target clippy pass. Broader core all-target clippy encounters five
pre-existing lints in static-claim fixtures and destroy.rs's identity
multiply; no new fence-code lint is reported. Seven canonical replay
failures retain exactly the recorded baseline digest pairs; six pass.
Corpus checksums pass. Logs: /tmp/bedlam-fence-{core,game,integration}.log.

The producer's chase-camera cut and SFX 0x22/channel 3 remain unwired.
Other terrain sets' trigger mappings remain unimplemented. This closes
the Boot Camp shutdown behavior, not the complete combat/mission goal.

## False extraction on welcome pad blocked further movement

Continuing original after fence shutdown reached LASER FENCE and then
TELEPORTER tutorials. Native accepted pointer movement and fire, but
repeated ground clicks left the robot fixed near the starting wall.
Temporary input diagnostics reproduced state 3, alive with 5000 HP,
at (155648,573440) Q13 after entering the welcome pad. This was an
erroneous extraction waiting state, unrelated to weapon fire.

The old zone-1 extraction union included slots 18 and 24, which the
original dispatches to tutorials. Corrected to slot 16 with SP mission-1
and network-mode gates, anchored in RE-EXW-MISSION-ROOM.md. Removed all
temporary tracing. Fresh release /tmp/bedlam-exit-fix-live.log entered
through menus, dismissed welcome, fired, then continued north with
ordinary clicks. The camera advanced past the previous stopping point
and native raised LASER FENCE at the same landmark as DOSBox. Captured
ammo 830, score 255, cash 250; original remains at TELEPORTER. Reaching
and riding the teleporter remains next work.

Validation: the strengthened slot-18 core regression FAILS with the old
union (unexpected extraction relocation) and passes with the corrected
table. Core library 157 passes, including the real exit's mission and
network gates. Production controller journey checks movement after
welcome dismissal; its earlier path alone passed even with the old
table, so the targeted pad regression is the decisive regression.
Destruction integration 26 and mission-scene integration 3 pass; fmt,
release build and game/shell all-target clippy pass. Manifest clean.

Canonical controls still have six passes and seven failures, but S6's
actual digest CHANGED from 96db1e0fb3206c70 to 0747e20e9c628fba; its
existing expected digest remains 1f26e1343d7296d4. This follows the
EXW-backed extraction correction and is not represented as unchanged
baseline. Other six failed digest pairs are unchanged. No pins altered.
Logs /tmp/bedlam-exit-pad-{red,green}.log and
/tmp/bedlam-exit-fix-{journey,controls,clippy}.log retain the evidence.

## First teleporter: original and native arrive on the raised platform

Original: dismissed TELEPORTER, then clicked the round disk below the
raised platform. The robot materialized on the platform beside the red
statue, with ammo 962, score 560, cash 400 unchanged. Original stays at
that destination, process 1709668.

Native /tmp/bedlam-ride-live.log: fresh actual menu/Auto journey, welcome
dismissal, generator fire, continued north through LASER FENCE and
TELEPORTER. Clicked the round disk at window input (482,498) in the
captured camera position. The next capture shows the robot on the same
raised platform beside the statue, ammo 830, score 255, cash 250.
A subsequent ground click at (580,460) moves the robot and camera across
that platform, verifying the ride releases ordinary movement. The fast
boarding flash was not captured live; its bank/frame/projection are
verified by the rendering regression, not by this screenshot comparison.

Production now stages seven Boot Camp ride records from actual PAD
markers, loads TELEPORT.BIN before mission replacement, arms a free
ride on its pad, and advances it once per simulation frame. Arrival
settles height/probes and conditionally clears the destination platform.
The renderer consumes countdown without advancing it. Other zones'
ride tables, marker sound, and broader mission outcomes remain open.

Validation: core 158, game 193, renderer 33, shell 149 tests pass (one
shell test ignored). The new core test covers a normal command/frame
boarding, busy guard, exact tenth-frame arrival, origin-centered boarding
versus uncentered landing, one-layer water clear, and movement afterward.
Production controller confirms actual PAD marker/destination records.
Destruction 26 and mission-scene 3 integration tests pass. Canonical
controls retain six passes and the same seven failed digest pairs as
D251, including S6 actual 0747e20e9c628fba. No pins changed. Fmt,
game/shell all-target clippy, release build and corpus checksums pass.
Logs /tmp/bedlam-ride-{core,game,render,shell,controls,clippy}.log;
game library results are included in the controls log.

## Pressure-pad elevator is the next live blocker

Original continued from the teleporter destination toward the red square
in front of the long brown strip. PRESSURE PAD explains red means an
elevator is lowered. After stepping on it, the strip lowers and loses
its tall wall face. Native reaches that landmark but retains the raised
strip; no pressure-pad/door tick is wired. Both processes remain live.
Exact map/dispatcher/rectangle evidence and implementation acceptance
are recorded in RE-EXW-ELEVATORS.md. This unit is investigation only;
no elevator fix or broader mission completion is claimed. Read-only
corpus checksums pass before/after the PAD and targeted EXW inspection.

## Pressure-pad elevator implemented and crossed (D253)

Native process 1925321, log /tmp/bedlam-elevator-live.log: repeated the
actual menu/Auto, generator, tutorials and teleporter journey. Ground
clicks toward the red pad lower the long brown strip. Its wall face
vanishes, matching original process 1709668. Native then walks over the
strip into the scaffold area (final window input 702,432). Original,
using relative mouse movement from its pad position, likewise crosses
onto the scaffold side. Both captures show the lowered strip behind the
robot and the gold-letter scaffold beside it. This verifies passage, not
just a changed graphic. The fast intermediate animation was not captured;
its packed frame and sixteen-tick stack transition are regression-tested.

The final core tests include an empty raising stack and correct epilogue
placement after platform creep; the live build predates these two narrow
corrections, which do not change this Boot Camp lowering route. The final
release is rebuilt separately. No global mission completion is claimed.

Core library: 161 passed. Game library: 193 passed; actual-corpus elevator
gate: 1 passed, checking all eighteen tiles, DAT plane 7, settled bias,
and a 32-unit collision-height reduction through ordinary commands and
frames. Destruction 26 and mission-scene 3 tests passed earlier in this
unit. Canonical controls retain six passes and the seven D251 failed
digest pairs, including S6 actual 0747e20e9c628fba; no pins changed.
Renderer 33 and shell 149 library tests pass (one shell test ignored).
Game/shell all-target clippy, fmt, release build and corpus checksums
pass. Logs are
/tmp/bedlam-elevator-{core,game,clippy,presentation,build}-final.log;
control results are /tmp/bedlam-elevator-controls.log.

Next natural route: continue past the scaffold to subsequent pressure
pads, verify their behavior against DOSBox, then pursue the remaining
mission path. Camera/SFX, other zones and final outcomes remain open.

## Blue-pad approach regression and toxic-waste route

Continued both live processes from the scaffold. Both show PRESSURE PAD
with the blue/raise explanation, followed by TOXIC WASTE on continuing
north into the mottled pool area. Original 1709668 entered the pool after
dismissing that warning; native 1925321 reached its near lip and dismissed
the same warning. This is progression evidence, not damage/exit parity.
Multiple approaches around the blue marker did not produce a confidently
observed lift rise in either running game. Keep that live activation open;
do not infer it from the tutorial or from the passing first red-pad route.

Extended elevator_gate with actual PAD 2's six-tile raising transition
and a command-driven approach from (10,48,2) toward (10,44). The latter
first failed with the fixture's zero-filled angle thresholds: the robot
barely moved. Replacing that placeholder with GAMEGFX/SINTABLE.BIN, as
production loads it, makes the approach and both existing transitions
pass. This is a test-fixture correction, not a runtime movement fix. The
approach now verifies actual navigation into the pad before the settled
0x90 bias; the direct transition checks all six shifted word stacks.

Validation: elevator_gate 3/3, targeted clippy -D warnings, fmt and corpus
checksums pass. Logs /tmp/bedlam-blue-pad-approach.log and
/tmp/bedlam-blue-pad-clippy.log. Runtime code and replay pins unchanged.
Next: compare toxic-floor damage and the route out of the pool; retain
blue-pad live raising and camera/sound verification as open work.

## Toxic damage producer and consumer fixed (D254)

Original was observed playing a movie after remaining in the pool;
native remained alive in that pool. We did not capture the exact original
exit transition. Native source and actual-map tests independently exposed
two defects: no correct toxic markers, and no phase-zero marker consumer.
See RE-EXW-TOXIC.md for the verified executable addresses and table bytes.

Corrected both. An idle robot on actual Boot Camp toxic floor loses 15 HP
per frame: 4985 after one, 5 after 333, dead with debris after 334. Core
regression checks nonzero phases, shield absorption/decay, transit immunity,
non-toxic clamp tiles and no repeated death tail. Stamp regression checks
inclusive endpoints, an outside word, and higher-layer precedence.
Core 162, game 193, destruction 26, elevator 3, mission-scene 3 and toxic 1
pass; targeted game/shell all-target clippy, fmt and release build pass.
Corpus checksums pass. Logs /tmp/bedlam-toxic-{core,gates,clippy,build}.log.

Canonical controls: three pass, ten fail. Original seven failures retain
their previous actual values except S7 (now f419634e99bda667, previous
7cbedbb21b2f2a19, expected 5d7217beb232d64b). Newly failing S5 actual
b34ce7d483476107 vs expected 0e844d916452ee78; S5b actual e1c5174b4b2bdea2
vs 288f9d39f602bd82; S5c actual f5c199a7400d9e7f vs 84b88562afa6fa54.
Their canonical harness stamps the corrected hazard grid at canonical.rs
1046. Expected pins unchanged. See /tmp/bedlam-toxic-controls.log.

Patched live replay is NEXT: running native PID 1925321 still uses the
pre-fix executable and stands in the pool. Final release is built; restart
through the menu and repeat the established route to observe damage and
then test the pool exit. Original PID 1709668 is still live, now outside
the mission. Full failure/debrief behavior is not fixed by this unit.

## Patched toxic death confirmed live; failure transition missing

Fresh release 46fd941, /tmp/bedlam-toxic-live.log, normal menu/Boot Camp
journey. Auto first chose Needler/Bouncy/Sticky/Hades, so restarted and
purchased Plasma Cannon X2 directly: category window input (375,570),
second popup row (375,590), BUY (785,592), DONE (871,702). Entered with
600 ammo and 3500 cash. Generator fire left 238 ammo, score 255. Repeated
the teleporter and first red-pad crossing, dismissed blue-pad and toxic
warnings, then entered the pool with two clicks (650,425).

The next capture shows the robot gone and the selected portrait static.
The mission remains displayed afterward and a ground command cannot
restore the dead robot. This confirms live toxic death, not a surviving
exit from the pool. No intermediate HP-bar measurement was captured.
The missing failure sequence is now anchored in RE-EXW-MISSION-FAILURE.md.
Original's movie is identified from EXW as GAMEGFX/GAMEOVER.SMK; original
live process remains outside the mission. Next implement and compare the
wipe/movie/return path; do not call this a completed mission outcome.
Read-only corpus checksums pass before/after the bounded EXW reads.

## Live game-over lifecycle restored (D255)

Rebuilt native /tmp/bedlam-failure-live.log: fresh New Game, Boot Camp,
manually purchased Plasma Cannon X2 (600 ammo), generator fire, teleporter,
red-pad crossing, blue-pad warning, toxic warning, then two pool clicks
(650,425). The robot dies and the scene leaves the mission for movie
playback. After playback it reaches Title; Escape dismissed the title
replay and exposed the normal menu. The short wipe was not captured in
individual live frames. Its frozen source, shrink geometry, timing and
survivor behavior are covered by focused tests. The earlier DOSBox pool
failure also returned to movie playback; EXW independently anchors the
GAMEOVER.SMK call and outer-game return.

Validation: game 197 and shell 149 library tests pass (one shell test
ignored). Elevator 3, mission-scene 3 and toxic 1 gates pass. Game/shell
all-target clippy, fmt, release build and corpus checksums pass. Tests
exercise the twelve-increment boundary, last eligible survivor selection,
manual cancellation, extraction/network/death-flag gates, frozen viewport
and preserved sidebar, host-owned transition, movie completion and held
input without campaign advancement. Logs /tmp/bedlam-failure-tests.log,
/tmp/bedlam-failure-gates.log, /tmp/bedlam-failure-clippy-final.log.

Canonical controls remain three passes/ten failures. Nine failed digest
pairs are unchanged from D254. S3 now reaches real GameOver before the
requested 133-record capture; it returns a typed early-mission-end error
instead of panicking at frame_counter_now. Expected hashes remain unchanged.
See /tmp/bedlam-failure-controls.log and /tmp/bedlam-failure-s3.log. Successful
extraction, high-score entry and full campaign return parity remain open.
Next resume a fresh original/native run toward the safe pool crossing and
subsequent pads, keeping blue-pad live raising verification open.


## Original blue-pad activation confirmed on fresh replay

[verified live, 2026-09-06] Original DOSBox process 2030137 was relaunched
from the existing /tmp/bedlam-original-play/BEDLAM scratch copy after the
previous process had closed. The title menu accepted a one-second left
press with the cursor over New Single Player Game. Boot Camp selection
and the Armoury door then accepted ordinary relative-pointer clicks.
STANDARD began with 3500 credits; selecting and buying Plasma Cannon X2
cost 500 and delivered 600 rounds. No Auto loadout was used.

Destroying the large blue-roof structures produced score 240 but left
the fence active. Shooting the small electrical generator subsequently
produced score 255, removed its poles and displayed LASER FENCE
DEACTIVATED. This separates the actual trigger from nearby scenery;
shooting the large structures alone is not evidence of fence shutdown.
The teleporter and first red-pad crossing were repeated naturally.

The blue pad now has direct original activation evidence. In the
800x629 window screenshot, approaching the blue marker first displayed
PRESSURE PAD. While that panel was visible, the marker was at roughly
(374,358), and the cursor at (470,386). A relative pointer movement
(-61,-18) with left click dismissed the panel; the continuing walk reached
the pad. The subsequent screenshot showed SECTION RAISED in the sidebar
and the brown strip raised. These are screenshot coordinates and relative
mouse deltas for this scratch-session configuration, not portable input
coordinates. The corresponding PAD 2 -> rectangle 1 action is independently
anchored in RE-EXW-ELEVATORS.md.

Native process 2016015 also repeated the teleporter, red-pad crossing and
blue tutorial. From the established route through native window clicks
(702,432) and (735,548), a click at (600,500) dismissed the tutorial and
continued toward the marker; (546,470) then moved onto the brown strip.
The native graphic is consistent with the raised strip, but this capture
alone does not independently establish its before/after height. Keep
native live raising verification open; the three actual-map elevator
regressions already cover the six-cell shift and normal approach.
Native also retains generator/roof graphics after fence shutdown, unlike
the original. Object picking and destruction presentation remain open.

The original next entered the toxic pool after dismissing TOXIC WASTE.
A far upper-right order reached the far wall instead of an exit; the
sidebar showed UNIT 1 IS TAKING HITS and UNIT 1 POWER AT CRITICAL. The
robot died before the attempted correction. Subsequent input reached
the saved-game menu after the failure return. This is a failed crossing,
not evidence of a safe route or a newly verified failure-movie sequence.
Native remains alive by the blue pad, before entering the pool. Next
establish the pool exit and issue the crossing route without stopping
inside it. Read-only corpus checksums pass; no runtime code or canonical
pins changed in this comparison unit.


## Pool ramp route regression and live retry

[verified corpus/native simulation] Examining the original DAT locates
entrance ramp bytes 0x23/0x24 at x=8..9,y=40..41 and exit ramp bytes
0x1d/0x1e at x=12..13,y=36..37. A real-map command regression now starts
at PAD 2 and traverses tile-center waypoints (10,44), (9,42), (9,40),
(12,37), (14,37). Each command must finish within one tile of its target
and the robot must survive. The crossing must incur damage, end at z=63,
and retain its remaining health through another 334 idle frames, longer
than the existing stationary-pool death budget. This proves a traversable
simulation route and a non-toxic endpoint; it does not prove original
or native window input parity.

[verified live] Native process 2016015 continued from the preceding
blue-pad position. Window input (585,428) displayed TOXIC WASTE; the same
click dismissed it and continued onto the entrance. Input (740,506)
reached the opposite pool lip. The robot died before the following
(618,490) order established a full exit; that later input landed on a
menu. Do not describe the lip capture as a successful crossing. The
next attempt should pause the test process between observations so
reasoning/tool delays do not consume the short toxic-health budget,
then verify the robot reaches the platform and remains alive there.
The original remains at its saved-game menu after the previous failed
attempt; it needs another natural Boot Camp start.

Validation: toxic_gate 2/2 passes, including the existing idle death test;
targeted clippy -D warnings, fmt, diff check and corpus checksums pass.
Logs /tmp/bedlam-pool-route-final.log and
/tmp/bedlam-pool-route-clippy.log. Runtime behavior and canonical pins
are unchanged. Full live crossing in both games remains required.


## Live gameplay diagnostics (D256)

The next paused live retry, process 2077199, again reached the far pool
lip but did not exit before dying. SIGSTOP/SIGCONT between captures
prevented idle tool time from continuously draining health, but the
wall/ramp overlap still made screenshot-only position estimates ambiguous.
Do not count this as a successful crossing or a proved projection bug.
The prior process 2016015 also ignored Main Menu clicks; its executable
hash differed from the current release, and focus-loss clearing already
exists in the source. No speculative mouse-latch fix was made.

Added `--trace-gameplay` to the window host. Example:

```sh
./target/release/bedlam-shell game-data/BEDLAM --window --classic --no-music-cache --trace-gameplay 2>/tmp/bedlam-trace-live.log
```

The opt-in stderr records carry the `bedlam-gameplay` prefix. They report
scene, actual input frame, mission frame, canonical cursor, height-adjusted
camera, robot tuples `(id, Q5-x, Q5-y, z, hp, alive, state, target)` and
nonzero elevator tuples `(linear tile index, packed bias)`. Records emit
on active input, every 60 mission frames, and scene changes. This is
read-only diagnostic evidence, not a replay format, input injection or
product-completion gate. It is disabled by default.

Fresh process 2091247 runs the traced release, with /proc/PID/exe and the
on-disk release both SHA-256
6b2197d4bb8cd519239ede33e857d819a3589de39077cb4b3ca4787a7ae9d722.
Actual window input selected Boot Camp, entered the Armoury, bought
Plasma Cannon X2 and clicked DONE. The trace records Mission frame 0,
robot `(0,687,2351,31,5000,true,0,None)`, and the following input frame.
The process remains at mission start with the purchased weapon; continue
its natural route using this trace to identify the pool exit precisely.
Original DOSBox 2030137 still needs a fresh Boot Camp start.

Validation: shell library 149 passed, one ignored; all-target shell
clippy -D warnings, fmt, release build, diff check and corpus checksums
pass. Logs /tmp/bedlam-trace-{tests,clippy,build}.log and live log above.
No simulation rules or canonical pins changed.


## Both live games cross the pool and activate the green pad

[verified live] Traced native process 2091247 completed the natural
Armoury/generator/teleporter/red-pad journey again. The trace exposed
camera-dependent click drift and a near-pad stopping point, not an
unimplemented blue-pad action. An order beyond PAD 2 reached it: frame
15426 has all six lift bytes 129, and frame 16260 has all six 144 (0x90),
with robot (336,1440,z63), hp5000. This closes native live blue raising
verification alongside the original SECTION RAISED capture.

Native's pool attempt then stopped at (400,1216,z33), the side of the
exit ramp, and could not move north from that edge. Ordering farther
east kept y=1216 and left the robot below the wall. Correcting westward
to (360,1192,z33), then ordering east through the ramp, succeeded:
frame 18532 shows (392,1192,z36,hp530), frame 18540 shows
(448,1192,z63,hp530) and the green-pad tutorial. Frame 18900 retains
530 health, and subsequent idle frames remain stable. Thus this is a
live safe exit, not the earlier ambiguous lip capture. SIGSTOP/SIGCONT
was used between observations; both processes were resumed afterward.

The original was restarted as process 2106527 from the same scratch
copy. STANDARD, manual Plasma Cannon X2 (500 credits/600 rounds), the
generator, first teleporter, red pad and blue pad were traversed again.
New Game accepted a cursor slightly below the label and a held press;
menu click timing remains an observation, not a claimed native fix.
After the TOXIC WASTE panel, the original cursor was approximately
(342,282) in the 800x629 screenshot. Dismissal entered the near ramp;
relative pointer delta (115,33) with click crossed toward the exit's
western approach. The next delta (-76,42) with click climbed the exit
ramp and displayed the same green-pad tutorial. Original process pauses
between these captures avoided idle tool time inside the pool.

Dismissing that original tutorial continued onto PAD 3: SECTION RAISED
and SECTION LOWERED appeared together, and the robot walked onto the
joined brown platform. Native window click (609,546) from its tutorial
position likewise reached PAD 3. Trace frame 66600 records the two
animations in progress (133 versus 5); frame 67800 records the ten
raising cells at 144 and ten lowering cells at 16. Native ends at
(532,1200,z63,hp530), original visibly at the same platform landmark.
Later screenshots and trace confirm both remain alive after waiting.
Original cash3000/score360 and native cash3500/score255 differ: difficulty
and shot histories were not normalized, so this is route/lift evidence,
not score, damage-budget, timing, enemy or pixel parity.

Added an actual-map PAD 3 regression: approach from (14,37), issue one
ordinary command across both rectangles, verify all twenty shifted
stacks and packed settled bytes, then require the live robot to reach
past x=19*32 at z63. Elevator tests 4/4 and toxic tests 2/2 pass;
targeted clippy -D warnings, fmt, diff check and corpus checksums pass.
Logs /tmp/bedlam-green-pad-{tests,clippy}.log. No runtime or replay pins
changed. The original/native live pool-crossing and first three pad
checks are now established. Continue from the joined platform toward
the later Boot Camp obstacles and exit; enemy staging, remaining weapon
families, camera/audio cues, other zones and mission victory remain open.


## Road sentry after the paired lifts — D257

Continued both live games beyond PAD 3 toward the glass structure. Original
DOSBox PID 2106527 is SIGSTOP-paused beside the opened sentry on the
striped road pad; its screenshot shows the barrel raised and turned.
Its captured cursor is approximately (88,245) in the 800-wide image.
Native PID 2091247 remains the previous binary, alive at Q5 (540,1048,z31),
hp530. The old native has no animation/fire producer. The three actual
Boot Camp TRT coordinates are (14,15,1), (11,15,1), and (10,33,1).

Implementation now connects the producer and corrects its kind-0x66
projectile dispatcher, as anchored in RE-EXW-SENTRIES.md. The new window
binary is built but has NOT yet been played to this landmark. Next live
unit must restart native, repeat the natural route, and compare opening,
aiming and firing against the paused original. Do not confuse PID
2091247's old executable with the rebuilt on-disk binary.

Validation: core library 167 passed, game library 197 passed, shell library
149 passed/one ignored; weapon-fire 28, sentry 1, elevator 4 and toxic 2
passed. Five focused sentry tests cover opening/fire/retraction, inactive
records, dispatcher step count/arming, robot contact without direct damage,
and damage at the contact tile after rollback. The actual-map sentry test
uses neighboring road tile (12,34); (12,33) has an existing one-point-per-
frame health loss and is not a neutral health fixture. That local terrain
behavior remains to be compared during the live journey.

Production library/binary clippy with -D warnings and fmt pass. Broader
all-target clippy finds four pre-existing lint errors in the static claim
bank differential test/data (type complexity, needless borrow, manual
range). Removed the trivial identity multiply in the touched destroy test.
No lint suppression was added. Corpus checksum remains clean.

Canonical controls remain 3 passed/10 failed, with pins unchanged. S3 still
ends early in GameOver. Newly changed actual hashes after the TRT connection:
S4 bfd7444be972e579; S5 6812e4ff3dc2f3eb; S5b 5b23daf652d3dc91;
S5c bdcba32c517f4766. Other actual hashes match the prior recorded run.
These cases stage the world/TRT bank; their broader drift remains open.


## D257 rebuilt live replay and debris correction

[verified live] Fresh PID 2151744 runs the rebuilt binary, SHA256
19ad6207282a733fb5592116e0cec7bca36427721f24ce6ae6acf8289d901adb,
confirmed against /proc/2151744/exe and target/release/bedlam-shell.
Trace: /tmp/bedlam-trace-sentry-live.log. STANDARD, manual Plasma Cannon
X2: entered with 600 rounds and 3000 cash. Fired at the small generator
poles, leaving 418 rounds, score15, fence cleared; large buildings stayed
intact. Ordinary input around them reached the teleporter, PAD1, PAD2,
toxic pool and PAD3. This is a fresh natural window journey, not a state
load or synthetic mission action.

The pool exit first reached (444,1208,z62), hp3035, frame19140. At
(464,1208,z63), the robot could not continue east/north onto the pad.
Backed west to (376,1200,z33), then north to (372,1172,z33), then across
the ramp to (456,1180,z63), hp1370. This upper line allowed movement to
(520,1196,z63) and activated both PAD3 lifts, then crossed to
(640,1196,z63). The road required x656 before moving north past y1132;
arrived (656,1048,z31). These edge catches remain comparison targets,
not proof that movement/collision is globally correct.

At (512,1048,z31), the road sentry was closed. At (412,1048,z31), it
visibly opened on the striped pad and fired. A lane movement then caused
secondary debris hits and pushed the robot to (472,1064), hp1338; the
sentry visibly retracted outside range. A further approach reached
(352,1028,z31), hp1300, with the gun raised/turning/firing and the glass
structure visibly breaking apart. Health then remained stable there.
Original PID2106527 was resumed and observed with the raised firing gun
and already broken glass structure; its turret changed heading as the
robot moved around it. This supports the producer/render/terrain-damage
connection. It does NOT establish exact damage, timing or knockback
trajectory parity between the two runs.

The live HP loss prompted re-reading the disburser, K8 stager and physics
call chain. RE-EXW-SENTRIES.md now corrects the earlier zero-damage claim:
no direct projectile damage, but two possible 2-point debris ticks plus
knockback. The old (12,33) test's 32-point loss was impact debris, not an
independently demonstrated terrain hazard; the earlier paragraph above
is superseded on that point. The focused regression now checks health
before disbursal physics, after each of two ticks, and after expiry.

Current continuation: native PID2151744 alive at (352,1028,z31), hp1300,
418 rounds, cash3000, score15. Original PID2106527 SIGSTOP-paused alive
beside the broken glass structure after two relative-pointer moves
(95,50 click), (108,28 click) from its prior paused cursor. Original
still has 496 rounds, cash3000, score360. Different firing histories
explain why these are not matched score/ammo captures. Continue both
along the road toward later Boot Camp pads; preserve the original as
reference. Five sentry tests and production core clippy pass; no runtime
code changed in this follow-up. Manifest checks clean.


## Hidden pads and five-level scaffold descent

[verified live] Continued native PID2151744 from the road sentry west
with ordinary input. At frame58140, Q5 (220,1020,z31), hp1296, the
HIDDEN PADS tutorial appeared. Original PID2106527 also reached the same
HIDDEN PADS text after moving away from the gun. Its HUD displayed
UNIT 1 IS TAKING HITS, consistent with the earlier secondary-impact
observation, but no exact original health measurement was taken.

After dismissal and approaching the scaffold, the original displayed
SECTION LOWERED and its three-column wall visibly descended. Resumed it
in bounded intervals until all five levels had lowered and its pending
movement entered the opening. Native also lowered the three stacks,
recording packed bias80 at cells804/829/854, Q5 (172,1012,z31), hp1296.
Moved native south to y1036 to avoid the northern pillar, then west
through the lowered section. It raised POWER UPS at (124,1036,z31).
After dismissal it crossed to x44, then repositioned to x84 inside the
corridor. The original is also visibly inside the scaffold opening.
POWER UPS has only been captured in native so far; no claim of an
identical tutorial trigger sequence beyond HIDDEN PADS.

The new actual-map elevator regression starts east of hidden PAD6,
walks through it and verifies all three stacks shift down five levels,
packed bias80, surviving robot crossing west of x4 and ground z31.
All five elevator tests pass, targeted clippy -D warnings and fmt pass,
manifest checks clean. No runtime implementation changed in this unit.

Current continuation: native PID2151744, /tmp/bedlam-trace-sentry-live.log,
at (88,1104,z31), hp1296, movement target (84,1164), obstructed among the
gold structures/pillars. It cannot continue north from y1004 because the
map has water beyond that end of the corridor. Original PID2106527 is
SIGSTOP-paused inside the same scaffold area; last screenshot shows the
robot beside a pillar and gold structure, cursor blue around (382,550)
in the 800-wide image. Its relative-pointer session93564 remains live.
Next test the scaffold route and gold structures in the original before
assuming a native collision bug or choosing destruction as the solution.
The actual PAD file places later pads at (2,14,1), (18,13,0), five return
teleporters along y1, and the exit at (17,25,4); these are navigation
facts, not evidence those later paths work.


## Gold pickup collection exposes the indexing bug — D258

[observed original] At the first scaffold gold structure, aimed at its
upper part (cursor325,376 in 800-wide screenshot), held Plasma X2 fire
for one second: ammo496 ->474, impact bursts, structure remained. Then
moved the cursor down by 28 relative units and left-clicked its floor.
The robot moved through, the gold structure disappeared, and cash rose
3000 ->3010 while score stayed360. Original PID2106527 is SIGSTOP-paused
after this collection. Cursor is now approximately (325,415); robot is
farther south along the gold row, nearest gold item gone.

[diagnosed] Actual x3/y35/level1 has DAT3 and TOT81..84. Native's
subtract-one lookup selected the wrong A/B range and never cleared it.
RE-EXW-PICKUP-INDEX.md (8397caa) re-anchors the literal raw-cell reads;
7h.5's old table-shape deduction is explicitly superseded. Corrected
production consumer uses raw set1 and floor48f. The actual-map movement
test collects the gold, awards score/money, clears DAT, writes floor/seen,
and passes into its former collision volume. This is an engine test;
rebuilt native has NOT yet been played through this pickup.

Validation: core library167, game197, shell149/one ignored; core corpus4;
gold pickup1, elevators5, toxic2 and sentry1 pass. Production lib/bin and
targeted regression clippy -D warnings, fmt and release build pass.
Manifest clean. Raw set2 also recognizes eight B-range cells omitted by
the old census (now160 in-range: 3 reinforcement,3 shield,6 health,
140 gold,1 booster,6 ammo,1 episode). Ammo/episode bodies currently return
effect IDs without full behavior and need implementation as encountered;
no claim that the indexing fix alone completes all pickup effects.

Canonical controls remain3 pass/10 fail, pins untouched. New actuals:
S5 dbba2c36049e6e49, S5b49b4ec440408f326, S5c1bea38c38c45b691;
other actuals unchanged, S3 still early GameOver. No product gate flipped.

Continue by restarting native PID2151744 (still old binary, at
(52,1104,z31), hp1296, target cleared) and replaying the known route to
verify gold collection live against paused original PID2106527. The
current source/build contains D258; do not mistake the live old process
for that build. Trace file remains /tmp/bedlam-trace-sentry-live.log.


## D258 rebuilt-window pickup verification

[observed, fresh production input journey] Rebuilt native SHA256
26e7a66c171fb41af43e0c6a9e128d5d4ae68585919bc8e6e49f58502c3606ef
was replayed from Title, STANDARD, Boot Camp and manual Plasma X2 purchase
(600 rounds, cash3000). Generator destruction consumed182 rounds and
awarded15 score. Teleporter, lower PAD1, raised blue PAD2, toxic crossing,
dual green PAD3 and the hidden five-level scaffold all passed again.
The pool route (304,1264) -> (348,1184) -> (448,1184), in Q5 coordinates,
exited at z63 with hp2840. Approaching the road sentry and scaffold cost8
further HP. This route avoided the previously troublesome y1208 exit line.

From the POWER UPS hint at (124,1048,z31), a ground click toward the gold
row ended at (108,1140,z31), hp2832, with cash3030 and score6015. Gold
images disappeared and the robot entered the previously blocking volume.
Thus collection and collision clearing are now verified in the actual
window, not solely the corpus regression. This approach collected several
pickups; its award totals do not prove equal RNG state or award parity with
the earlier original single-pickup observation (cash+10, score unchanged).

Current native PID2373747, exec session45052, remains alive at that point;
trace /tmp/bedlam-pickup-live2.log. The earlier restarted session had the
wrong default-cannon loadout and was discarded before this controlled run.
Original PID2106527 is now absent, no DOSBox process/window remains, and
the retained log ends with SIGTERM. Its earlier paused location is no longer
resumable. Replay the original from the existing scratch install for the next
live comparison; do not claim a new simultaneous award comparison. Corpus
manifest checks passed before and after this playthrough. No code or pins
changed, and no product gate is certified by this bounded live verification.


## Restored original reference and manual-loadout cross-check

[observed original] Relaunched the existing scratch install after verifying
that no DOSBox process/window remained. New reference PID2400491, exec
session85982, log /tmp/bedlam-original-play/reference-replay.log. Replayed
Title -> New Single Player -> Boot Camp -> Armoury -> manual Plasma X2
-> BUY -> DONE. The original visibly confirms cost500, quantity600,
balance3500 ->3000, and mission entry with PLASMA CANNON X2 0600,
score0/cash3000. This independently matches the rebuilt native's manual
loadout used in the D258 journey; no AUTO-loadout or exact RNG claim.
Original is SIGSTOP-paused at the initial robot location, before movement.

Input reliability: menu movement and click must be separated when the
original has not yet processed the pointer movement. A one-second physical
button hold registered reliably where the earlier short hold only hovered.
New persistent relative uinput helper /tmp/bedlam-reference-pointer.py runs
in exec session64458. Commands are `dx dy` for movement, then a separate
`0 0 click` (one-second left hold), or `0 0 right`. Current cursor is about
(737,603) in the 800-wide original screenshot. Menu-relative gain differed
across scene entry; in Armoury it settled at approximately1.56 screenshot
pixels per input unit. Inspect after moving before clicking narrow rows.
The earlier session93564 still exists but is not the preferred helper.
Continue the original tutorial route from the start to regain a direct
pickup-corridor comparison; native PID2373747 remains at (108,1140,z31).
Manifest clean; reference execution mounts only the scratch copy.


## Restored original reaches the toxic-pool entrance

[observed original, continued PID2400491] Completed the generator/fence,
first teleporter, brown lift crossing, blue pressure-pad raising and TOXIC
WASTE tutorial on the restored reference. Generator fire used20 Plasma X2
rounds (600 ->580) and score rose0 ->15; cash stays3000. Native's generator
award also was15, but its longer firing hold prevents an ammunition/timing
comparison. The original generator tutorial visibly cuts the camera to the
building/generator area; native's missing scripted camera cut remains open.

PID2400491 is now SIGSTOP-paused at the black platform tip on the pool's
entrance side, after dismissing TOXIC WASTE, before entering toxic ground.
Sidebar health bar remains full, Plasma580, cash3000, score15. Cursor is
approximately(367,269) in the 800-wide original screenshot; robot is near
(300,320), pool extends right and up, exit is farther right. Native remains
at the collected gold corridor. Continue the original across the pool.

[control refinement] One-second held clicks reissue ground targets as the
camera moves and overshoot pads. Keep the long-hold helper for menus only.
For gameplay, move with session64458, inspect, then send a brief physical
left hold (0.08 seconds via evdev worked). Existing session93564's0.3-second
click also moves but overshot small pad approaches in this replay. The blue
pad raised after a brief click/dismissal and its brown platform was crossed.
Manifest remains clean; no engine or canonical changes in this checkpoint.


## Restored original returns to gold; extended native corridor

[observed original] PID2400491 crossed the toxic pool alive, reached the
exit ramp and green PRESSURE PAD tutorial, activated the paired lifts,
and crossed the joined brown platform. Continued along the road: sentry
fired and broke the glass structure again. HIDDEN PADS appeared, the
five-level scaffold lowered, and POWER-UPS then appeared inside the passage.
This closes the earlier missing original observation of that tutorial at
this passage, without proving frame-exact trigger/camera timing parity.

Walking into the gold row cleared several gold images. Score15 ->22015
and cash3000 ->3010, Plasma580 unchanged. The earlier original cash-only
single collection therefore was not a fixed reward for every gold pickup;
this replay demonstrates both score and cash awards. Native also awards
both categories. The paths and RNG histories differ, so exact amount/RNG
parity remains unproven. Reference PID2400491 is SIGSTOP-paused inside the
row after that collection, cursor approximately(250,385) in an 800-wide
screenshot. It has regained the live comparison location; no restart needed.

[observed rebuilt native] Continued south through the gold row from
(108,1140,z31) to (112,1256), (112,1368), (116,1484), then (120,1488),
all Q5/z31, hp2832 stable, Plasma418. Score now33015, cash3040. Current
order targets approximately(118,1614) but remains blocked near a scaffold
pillar; correct the lateral approach and compare the reference as needed.
The corridor runs back toward the earlier brown lift; it has not yet proved
a new main-route exit. Do not infer global traversal from gold collection.
Native PID2373747, trace /tmp/bedlam-pickup-live2.log; original log and
pointer sessions unchanged. Manifest clean. No canonical or product flips.


## Gold corridor pillar approach resolved

[observed native + corpus inspection] The stalled (120,1488) approach was
offset from the one-tile passage. Raw Boot Camp DAT has solid pillars at
x2/x4 on rows47 and50, with the gold path at x3. Moving west to x92 also
blocked; centering at x112 before moving south passed through to
(108,1600,z31), hp2832, score33015, cash3410, Plasma418. No collision
change was needed. The remaining nearby gold cleared on the centered run.
This establishes traversal of these two narrow gaps in the actual window;
it does not establish an exit through the solid row51 farther south.

Added a real-corpus regression spawning at the x3 lane center on row46,
walking through row50, checking the row47/48 gold clears while both pairs
of solid pillar cells remain intact. It protects passage clearance without
making the pillars walkable. Native PID2373747 now stands at (108,1600).
Reference PID2400491 remains paused at the earlier collected-gold checkpoint.

Validation: both pickup corpus regressions pass in release; targeted clippy
with -D warnings and cargo fmt pass; queue remains six runnable items.
The regression destination is eight Q5 units beyond the tile center to
allow normal stopping distance while proving entry into the second gap.
Manifest clean before/after corpus tests; no engine behavior changed.


## Main route north of the extraction archway

[observed native] Returned north through the cleared gold corridor from
(108,1600) to (108,1036), exited the lowered scaffold east to (236,1036),
(364,1036), then (416,1036). Moving north reached the extraction tutorial
at (416,988): find the extraction point marked by a beacon, pass under the
archway or use a teleporter. Dismissed and followed the unobstructed x416
lane through (416,872), (416,756), to (416,640), all Q5/z31. HP2832,
Plasma418, cash3410 and score33015 stay unchanged. The roof occludes the
robot during traversal; it emerges into the northern area containing another
fence generator, green fence, plants and two sentry pads. These new obstacles
are not yet live-verified. Native PID2373747 remains alive at (416,640).

[corpus route inspection] Raw DAT x12/x13 has open level1/2 across rows23
through30 between the large structures, consistent with this underpass.
This is navigation evidence, not a new RE behavior claim.

[observed original] Returned from the gold passage to the road and reached
NOW YOU ARE ON YOUR OWN / find extraction / archway-or-teleporter tutorial,
matching the native instruction and route choice. Original was already low
on health after pool and sentry exposure. Continued sentry fire caused hits
while approaching the archway; the next observation was the title intro
movie, so this reference life ended before underpass traversal could be
verified. Do not claim completed original traversal or a saved mission
checkpoint. PID2400491 remains alive but SIGSTOP-paused in the intro movie;
its former gold checkpoint is gone. Native crossing is verified, original
crossing remains open. Manifest clean, no code or canonical pin changes.


## Northern generator, PAD8 lift and extraction roof reached

[observed rebuilt native] From (416,640,z31), firing Plasma at the northern
green generator removed the green fence, score33015 ->33030, ammo418 ->356.
This is the second Boot Camp shutdown mapping (type86 linked to83), now
live-observed; the generator sprite remains visually present afterward.
Advanced via (416,532) to DESTROYABLE WALLS at (416,444). The straight
north order stopped at (416,428), at a raised platform edge. One second of
fire toward the edge consumed62 more rounds and awarded75 score, with
secondary damage reducing HP to2684, but did not permit northward movement.
This does not prove the tutorial's destroyable-wall route was exercised.

Went west via (268,428) to (120,428,z63), using the visible western ramp.
Clicked blue PAD8 at (84,464,z63): four cells252,253,277,278 reached bias144.
Crossed the raised strip via (84,348), (84,232), then TELEPORTERS tutorial
at (84,124). Dismissed and walked toward the northern end. Gold collection
raised cash to3560. Entering PAD10 at the end teleported the robot to
(256,832,z159), on the extraction roof. Walked east via (380,832) and
(508,832), then triggered the congratulations/extraction instructions near
(512,832). Thus the ramp, PAD8 lift and PAD10 ride work in the actual window.
Original live comparison of this northern route remains outstanding after
its prior death; these observations alone do not certify parity.

[open blocker] After dismissing the extraction tutorial, a ground order
toward (568,816) stops at (528,816,z159), state1, against the beacon's
boundary. No successful outcome/debrief is observed. Investigate collision
and PAD16 triggering against original/EXW before changing either: PAD16 is
(17,25,4), whose tile begins at x544. Current extraction dispatch checks the
robot's occupied tile before movement; its current center stays outside.
Do not call this a successful mission. Native PID2373747 is alive here,
HP2684, Plasma294, cash3560, score33105; trace file unchanged. Original
PID2400491 remains paused in title intro, not in a resumable mission.
Manifest clean; no implementation or canonical pin changes this unit.

## Original extraction completed; D259 elevated-object restoration

[observed original] Replayed STANDARD with Plasma X2 purchased for500,
600 rounds. Several mis-aimed shots at machinery/fence endpoints preceded
correct targeting of the electrical-post generator, so ammo was460 after
its shutdown (score15). Correct landmark: POS type85 is(16,63,1); the pair
of electrical posts is the generator, not the adjacent red machinery.
Passed first teleporter/red/blue pads. Fast pool crossing preserved most
health: from the first toxic hint, short click at cursor(350,250), pause;
then relative(109,90) short click reached the right exit ramp. Relative
(-58,-14) reached green hint, (-60,10) dismissed/activated paired lifts.
Crossed brown platform and road, bypassing the optional gold scaffold.
Passed under the glass arch at the extraction instruction, then north
under the building into the green-generator area. Original has active
critters and substantial explosions here; native NME staging is still
missing. Shot the green electrical posts, used western ramp, activated
blue PAD8, crossed its raised strip and entered the northern teleporter.
Original arrived on the extraction roof alive, Plasma440, cash3250,
score180. Thus the previously unverified northern route is now observed.

Original congratulations hint appeared on approach to the tower. Repeated
left-click approaches stopped beside the intact beacon, just as native.
One second of right-button fire collapsed it, ammo440 ->420, score180
->220. The pending movement entered the cleared pad, the dropship arrived,
and EVACUATION COMMENCED appeared. After a few seconds the evacuation
movie played. A later observation found the mission room again, with the
next region highlighted. No input-injected completion, teleport or health
change was used. Original PID2400491 is SIGSTOP-paused in the mission room
now; it no longer holds a mission checkpoint. Relative input helpers93564
and64458 remain available. Original screenshot scale800x629, CU960x755.

[observed old native] Shooting the beacon at CU(550,510) plus one-second
right hold awarded40 (33105 ->33145), consumed60 (294 ->234) and reduced
HP2684 ->2544, but left the tower and collision intact. PID2373747 remains
at(528,816,z159), target(568,816), running the old D258 executable. The
new binary on disk does not change its memory; do not claim live repair.

[verified implementation] D259 changes destroy restoration's bank index
from world z to local z-z0. Original EXW uses the latter; the former skips
all three beacon layers at origin5. Original data: row196,1x1x3,hp400,
kind40, UNDER TOT(1331,0,0), UNDER DAT(0,0,0). PAD16 level4 survives.
New synthetic multi-cell/multi-layer test covers origins1/5/7 and upper
clamping. New actual-map extraction_gate dismisses the congratulations
hint through a command, proves intact collision, destroys the beacon,
checks all restored layers, walks into the pad, and reaches extraction
state(1,true). Both fail before the fix. The order record can be consumed
within the same frame; the persistent beacon latch/extraction counters are
the appropriate assertions, not requiring an intermediate order to remain.

Validation: destroy27, extraction1, elevator5, pickup2, sentry1, toxic2 all
pass. Library tests core167/game197/shell149 pass (shell1 ignored). Release
shell rebuilt, SHA256 b2b3af690d45241d7030a9ecb194770ea58d2e426ea40a82c052a50b0df5811f.
fmt and clippy-Dwarnings pass for production libraries/binaries and both
changed test targets. An all-targets clippy attempt finds four existing
warnings in core static_claim_bank_differential/data/claim_rects: two
complex tuple types, needless borrow, manual range check. They are outside
this change; no suppression was added. Manifest checks clean.

Canonical dump remains3 pass/10 fail, pins unchanged. Relative to D258,
S4 actual bfd7444be972e579 ->2148b08e16f205a9;
S5c 1bea38c38c45b691 ->ee2e6628c3f8cf8b;
S7 f419634e99bda667 ->6fabdbd982418efd.
Other actuals unchanged: S0 9660dca21e25fa46, S2 df4377749180cff4,
S5 dbba2c36049e6e49, S5b49b4ec440408f326, S6 0747e20e9c628fba,
S8 5bdb330f057a04d4; S3 typed early-GameOver capture error remains.
Logs /tmp/bedlam-d259-lib-tests.log and /tmp/bedlam-d259-canonical.log.
No product phase/ledger flips. Next: fresh rebuilt native playthrough and
mission-owned successful outcome, using the now completed original run
as the reference. The old native terrain cannot be repaired by another
shot: its object is already marked destroyed, so replay is required.

## D259 rebuilt window verification and successful-outcome gap

[observed, 2026-09-06] Fresh STANDARD Boot Camp through title, mission
room, player-selected Plasma X2 purchase and DONE, without state injection.
Native PID2584686, trace /tmp/bedlam-menu-diag.log. Both electrical
generators visibly collapse after fire; the toxic pool, green paired
lifts, northern road, western ramp, blue PAD8 and roof teleporter work.
Skipped the optional gold detour. At the beacon: (528,816,z159), hp2836,
ammo480, cash3030, score30, pending target(568,816). Right-click its base
at CU(550,510) then hold right one second: tower collapses, score70,
ammo422, hp2806, robot(544,800,z159) state5. This is live confirmation
of D259 restoration and normal-input extraction entry, beyond the focused
core test. Five further seconds leave Scene::Mission and state5 unchanged
(frame22800). No dropship art/movie or mission-room return appears.
Process is SIGSTOP-paused there; original PID2400491 remains paused in
the mission room after its completed evacuation. Old native processes
2373747/2568147 have exited.

[diagnosed menu observation] Apparent ignored title-menu clicks were
attract-timeout replay consuming the first click, not a hit-area defect.
Temporary gated menu logging showed clicked=true with phase=Attract.
Prompt second clicks selected correctly. Diagnostic source was removed;
this running executable still contains the logging-only instrumentation.
Rebuild from clean source before the next release verification.

[code evidence] GameHost::pump_frame handles mission_failed only after
MissionScene::tick. It never resolves successful extraction. The core
already sets extraction_complete after dropship departure; scene rendering
also lacks the observed craft. Outcome routing and craft presentation are
separate remaining defects; do not mark Boot Camp product-complete.

## 2026-09-07: original post-extraction inventory and D261 handoff

[observed] Continued original PID2400491 from the post-Boot-Camp mission
room. Selected B-2 (Operation Deaf Bat), opened Armoury and waited through
its entrance animation. Balance3250 and Plasma Cannon X2 remain present,
matching the cash/weapon at extraction. Attempts to sell the row did not
produce an observable transaction; no live resale amount is claimed.
Original is now SIGSTOP-paused in the B-2 armoury, not in the mission room.

[verified test] D261 controller boundary regression now reaches Cutscene
from natural craft departure, plays ZONEDONE once, finishes loading, opens
the Zone B room and launches B-2 with retained inventory/cash/score. It
uses an explicit near-beacon position fixture, so full window verification
is still required. Production cutscene ring playback was a second blocker:
without the one-pass bound it remains at Cutscene after2000 pumps.
Native PID2584686 still runs the old D259+logging executable at the
extraction rooftop; the rebuilt D261 executable on disk needs a fresh run.

## D261 full window route verified (2026-09-07)

[observed] Rebuilt cbdf4ab, native PID2650028, exec session72252,
/tmp/bedlam-d261-standard.log. Selected STANDARD in the title menu
(visible), Boot Camp, Plasma X2 purchase500/600 rounds, DONE. Followed
the same ordinary route through generator, teleporter, red/blue lifts,
toxic pool (hp2930 on exit), green paired lifts, underpass, northern
generator, western ramp, blue PAD8 and roof teleporter. Both generators
collapsed. At beacon (528,816,z159), hp2924, ammo480. Shot base at
CU(550,510) with one-second right hold: tower collapsed, state5 at
(544,800,z159), hp2894, then Scene::Cutscene without injected actions.
ZONEDONE visible, no skip click; after eight further seconds the Zone B
mission room was visible automatically. Selected B-2, opened armoury:
Plasma X2 and cash3000 retained. DONE launched B-2 normally. New mission
HUD: ammo420, score3070, cash3000, hp5000; robot(975,2319,z31).
Native is SIGSTOP-paused at that B-2 spawn. No player-position, health,
mission-outcome or campaign edits were used during this full window run.
Only process pauses between observations and normal controls were used.
The earlier SIMPLE startup PID2648474 was discarded before mission entry;
PID2650028 is the STANDARD run. Old PID2584686 has exited.

[observed original] Continued PID2400491 from B-2 armoury through DONE.
Original now also stands on the B-2 runway with the same plane/fence/gold
landmarks and Plasma X2 ammo420. Cash3250, score10220. SIGSTOP-paused
there. The score difference is not evidence of a completion bonus:
EXW0x40f1bf's +10000 is in the random gold-pickup award family. Earlier
small score readings may have missed the high digits or preceded the
strip refresh; award RNG parity remains unproven. Do not add a training
bonus from this comparison. Cash difference also arose during the gold
route; both games preserved their own outgoing cash. Original radar
already shows blue/white map targets; native B-2 radar is mostly empty,
a further visible mismatch to investigate.

D261 success transition is now live-verified. Missing in-game dropship
artwork/boarding disappearance, NME staging, other-zone objective results,
award RNG and the other documented gaps keep product parity incomplete.
Manifest checked clean after the corpus-backed runs.

### D262 rebuilt live comparison (2026-09-07)

Fresh STANDARD run on rebuilt d4af660 completed the full Boot Camp
route, shot both generators and the extraction beacon, watched ZONEDONE,
returned to the room and launched B-2. Blue fence bars now appear on
the B-2 radar at the original's matching runway location. White objective
markers remain missing; EXW table audit identifies the actual B-2 groups
in RE-EXW-MISSIONVIEW.md. The absence is objective staging, not another
fence-icon color rule. Native PID2688217 is paused B-2 frame120 at
(975,2319,31), HP5000, ammo420, cash3000, score3070; original PID2400491
remains paused at matching spawn. Log /tmp/bedlam-d262-live.log.
Corpus manifest clean before and after the live run and table reads.

### D263 objective markers and defended B-2 gate (2026-09-07)

Fresh STANDARD native replay on41e3192 completed Boot Camp naturally and
entered B-2 with ammo420/cash3000/score3070. At spawn (975,2319,31),
the white markers now overlay the two blue fence rows in the same layout
as the original. This verifies the objective-marker presentation at that
location; actual primary-target destruction is still untested live.

Both games were moved southwest toward the primary target. Native is
paused at (575,2579,31), HP5000, target(571,2623), against the green
fence at tiles(18,81)/(19,81). Original reaches the same road/fence
junction, but has walking enemies and active gunfire/explosions beside
the west wall; the native view has none. Inspection confirms no production
NME staging or critter drawing in MissionScene. Core stage_critters already
accepts all eight NME sections; integration must pair loading with rendering
so this fix does not introduce invisible enemies. This is the next gameplay
priority before treating the native route as a representative challenge.
Read-only POS cross-check: gate types0x67 at slots668/669. Their generator
is type0x70 at slot964, (66,14,1), not the nearby blue machinery. Do not
bypass this fence or invent a nearby control target.

Original MAP showed the objective-room overlay and unexplored terrain.
Pressing Escape there returned directly to the campaign room. Reselecting
B-2 and entering armoury showed balance3250 with no weapon in the owned
list. Buying PlasmaX2 again produced balance2750 and a new owned row;
DONE then relaunched B-2 with ammo600 and score10220. Thus this abort
sequence did not preserve the prior420 rounds, unlike successful Boot
Camp extraction. Abort caller semantics still need EXW confirmation before
implementation. Earlier same-coordinate MAP/DONE click attempts sometimes
missed their narrow hit areas; they are not evidence of broken buttons.

Current checkpoints: native PID2718912, log /tmp/bedlam-d263-live.log,
paused at the green gate; original PID2400491 paused at the corresponding
junction after its B-2 restart, ammo600/cash2750/score10220. Original
relative pointer is near screenshot(134,329), short helper session93564;
long helper64458 remains usable. Native leftward route from spawn used
CU(397,442) once, then CU(305,494) three times; the last move meets the
fence. Corpus manifest remained clean.
